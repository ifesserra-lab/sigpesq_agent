---
description: Run the full verification suite (tests + linting)
---

To ensure the library maintains its 100% quality target, run:

// turbo
1. Run linting and tests:
```bash
./.venv/bin/flake8 src tests && ./.venv/bin/pytest
```
